<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <import type="android.text.TextUtils" />

        <variable
            name="actorInfo"
            type="com.geekbrains.team.filmlibrary.model.PersonView" />

        <variable
            name="listener"
            type="com.geekbrains.team.filmlibrary.adapters.OnActorSelectedListener" />

    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_margin="5dp"
        android:onClick="@{() -> listener.openActorDetails(actorInfo.id)}"
        app:cardCornerRadius="4dp"
        app:cardMaxElevation="8dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/cover_iv"
                android:layout_width="68dp"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_no_poster"
                app:layout_constraintStart_toStartOf="parent"
                app:poster='@{actorInfo.posterPath}'
                tools:ignore="ContentDescription" />

            <TextView
                android:id="@+id/name_tv"
                style="@style/Headline3"
                android:layout_width="307dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="14dp"
                android:layout_marginBottom="6dp"
                android:ellipsize="end"
                android:gravity="bottom"
                android:lines="1"
                android:text='@{actorInfo.name}'
                app:layout_constraintBottom_toTopOf="@id/job"
                app:layout_constraintStart_toEndOf="@id/cover_iv" />

            <TextView
                android:id="@+id/job"
                style="@style/Subtitle2"
                android:layout_width="234dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="38dp"
                android:ellipsize="end"
                android:gravity="bottom"
                android:lines="1"
                android:text="@{actorInfo.character}"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="@id/name_tv" />


        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>